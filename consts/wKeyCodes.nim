const
  wKeyNone* = 0
  wKeyLButton* = VK_LBUTTON
  wKeyRButton* = VK_RBUTTON
  wKeyCancel* = VK_CANCEL
  wKeyMButton* = VK_MBUTTON
  wKeyXButton1* = VK_XBUTTON1
  wKeyXButton2* = VK_XBUTTON2
  wKeyBack* = VK_BACK
  wKeyTab* = VK_TAB
  wKeyClear* = VK_CLEAR
  wKeyReturn* = VK_RETURN
  wKeyEnter* = VK_RETURN
  wKeyShift* = VK_SHIFT
  wKeyControl* = VK_CONTROL
  wKeyCtrl* = VK_CONTROL
  wKeyMenu* = VK_MENU
  wKeyAlt* = VK_MENU
  wKeyPause* = VK_PAUSE
  wKeyCapital* = VK_CAPITAL
  wKeyKana* = VK_KANA
  wKeyHangeul* = VK_HANGEUL
  wKeyHangul* = VK_HANGUL
  wKeyJunja* = VK_JUNJA
  wKeyFinal* = VK_FINAL
  wKeyHanja* = VK_HANJA
  wKeyKanji* = VK_KANJI
  wKeyEscape* = VK_ESCAPE
  wKeyEsc* = VK_ESCAPE
  wKeyConvert* = VK_CONVERT
  wKeyNonConvert* = VK_NONCONVERT
  wKeyAccept* = VK_ACCEPT
  wKeyModeChange* = VK_MODECHANGE
  wKeySpace* = VK_SPACE
  wKeyPrior* = VK_PRIOR
  wKeyNext* = VK_NEXT
  wKeyEnd* = VK_END
  wKeyHome* = VK_HOME
  wKeyLeft* = VK_LEFT
  wKeyUp* = VK_UP
  wKeyRight* = VK_RIGHT
  wKeyDown* = VK_DOWN
  wKeySelect* = VK_SELECT
  wKeyPrint* = VK_PRINT
  wKeyExecute* = VK_EXECUTE
  wKeySnapshot* = VK_SNAPSHOT
  wKeyInsert* = VK_INSERT
  wKeyDelete* = VK_DELETE
  wKeyHelp* = VK_HELP
  wKeyLWin* = VK_LWIN
  wKeyRWin* = VK_RWIN
  wKeyApps* = VK_APPS
  wKeySleep* = VK_SLEEP
  wKeyNumpad0* = VK_NUMPAD0
  wKeyNumpad1* = VK_NUMPAD1
  wKeyNumpad2* = VK_NUMPAD2
  wKeyNumpad3* = VK_NUMPAD3
  wKeyNumpad4* = VK_NUMPAD4
  wKeyNumpad5* = VK_NUMPAD5
  wKeyNumpad6* = VK_NUMPAD6
  wKeyNumpad7* = VK_NUMPAD7
  wKeyNumpad8* = VK_NUMPAD8
  wKeyNumpad9* = VK_NUMPAD9
  wKeyMultiply* = VK_MULTIPLY
  wKeyAdd* = VK_ADD
  wKeySeparator* = VK_SEPARATOR
  wKeySubtract* = VK_SUBTRACT
  wKeyDecimal* = VK_DECIMAL
  wKeyDivide* = VK_DIVIDE
  wKeyF1* = VK_F1
  wKeyF2* = VK_F2
  wKeyF3* = VK_F3
  wKeyF4* = VK_F4
  wKeyF5* = VK_F5
  wKeyF6* = VK_F6
  wKeyF7* = VK_F7
  wKeyF8* = VK_F8
  wKeyF9* = VK_F9
  wKeyF10* = VK_F10
  wKeyF11* = VK_F11
  wKeyF12* = VK_F12
  wKeyF13* = VK_F13
  wKeyF14* = VK_F14
  wKeyF15* = VK_F15
  wKeyF16* = VK_F16
  wKeyF17* = VK_F17
  wKeyF18* = VK_F18
  wKeyF19* = VK_F19
  wKeyF20* = VK_F20
  wKeyF21* = VK_F21
  wKeyF22* = VK_F22
  wKeyF23* = VK_F23
  wKeyF24* = VK_F24
  wKeyNumlock* = VK_NUMLOCK
  wKeyScroll* = VK_SCROLL
  wKeyOemNecEqual* = VK_OEM_NEC_EQUAL
  wKeyOemFjJisho* = VK_OEM_FJ_JISHO
  wKeyOemFjMasshou* = VK_OEM_FJ_MASSHOU
  wKeyOemFjTouroku* = VK_OEM_FJ_TOUROKU
  wKeyOemFjLoya* = VK_OEM_FJ_LOYA
  wKeyOemFjRoya* = VK_OEM_FJ_ROYA
  wKeyLShift* = VK_LSHIFT
  wKeyRShift* = VK_RSHIFT
  wKeyLControl* = VK_LCONTROL
  wKeyRControl* = VK_RCONTROL
  wKeyLMenu* = VK_LMENU
  wKeyRMenu* = VK_RMENU
  wKeyLCtrl* = VK_LCONTROL
  wKeyRCtrl* = VK_RCONTROL
  wKeyLAlt* = VK_LMENU
  wKeyRAlt* = VK_RMENU
  wKeyBrowserBack* = VK_BROWSER_BACK
  wKeyBrowserForward* = VK_BROWSER_FORWARD
  wKeyBrowserRefresh* = VK_BROWSER_REFRESH
  wKeyBrowserStop* = VK_BROWSER_STOP
  wKeyBrowserSearch* = VK_BROWSER_SEARCH
  wKeyBrowserFavorites* = VK_BROWSER_FAVORITES
  wKeyBrowserHome* = VK_BROWSER_HOME
  wKeyVolumeMute* = VK_VOLUME_MUTE
  wKeyVolumeDown* = VK_VOLUME_DOWN
  wKeyVolumeUp* = VK_VOLUME_UP
  wKeyMediaNextTrack* = VK_MEDIA_NEXT_TRACK
  wKeyMediaPrevTrack* = VK_MEDIA_PREV_TRACK
  wKeyMediaStop* = VK_MEDIA_STOP
  wKeyMediaPlayPause* = VK_MEDIA_PLAY_PAUSE
  wKeyLaunchMail* = VK_LAUNCH_MAIL
  wKeyLaunchMediaSelect* = VK_LAUNCH_MEDIA_SELECT
  wKeyLaunchApp1* = VK_LAUNCH_APP1
  wKeyLaunchApp2* = VK_LAUNCH_APP2
  wKeyOem1* = VK_OEM_1
  wKeyOemPlus* = VK_OEM_PLUS
  wKeyOemComma* = VK_OEM_COMMA
  wKeyOemMinus* = VK_OEM_MINUS
  wKeyOemPeriod* = VK_OEM_PERIOD
  wKeyOem2* = VK_OEM_2
  wKeyOem3* = VK_OEM_3
  wKeyOem4* = VK_OEM_4
  wKeyOem5* = VK_OEM_5
  wKeyOem6* = VK_OEM_6
  wKeyOem7* = VK_OEM_7
  wKeyOem8* = VK_OEM_8
  wKeyOemAx* = VK_OEM_AX
  wKeyOem102* = VK_OEM_102
  wKeyIcoHelp* = VK_ICO_HELP
  wKeyIco00* = VK_ICO_00
  wKeyProcessKey* = VK_PROCESSKEY
  wKeyIcoClear* = VK_ICO_CLEAR
  wKeyPacket* = VK_PACKET
  wKeyOemReset* = VK_OEM_RESET
  wKeyOemJump* = VK_OEM_JUMP
  wKeyOemPa1* = VK_OEM_PA1
  wKeyOemPa2* = VK_OEM_PA2
  wKeyOemPa3* = VK_OEM_PA3
  wKeyOemWsctrl* = VK_OEM_WSCTRL
  wKeyOemCusel* = VK_OEM_CUSEL
  wKeyOemAttn* = VK_OEM_ATTN
  wKeyOemFinish* = VK_OEM_FINISH
  wKeyOemCopy* = VK_OEM_COPY
  wKeyOemAuto* = VK_OEM_AUTO
  wKeyOemEnlw* = VK_OEM_ENLW
  wKeyOemBacktab* = VK_OEM_BACKTAB
  wKeyAttn* = VK_ATTN
  wKeyCrsel* = VK_CRSEL
  wKeyExsel* = VK_EXSEL
  wKeyEreof* = VK_EREOF
  wKeyPlay* = VK_PLAY
  wKeyZoom* = VK_ZOOM
  wKeyNoname* = VK_NONAME
  wKeyPa1* = VK_PA1
  wKeyOemClear* = VK_OEM_CLEAR
